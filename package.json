{
  "name": "@mkbabb/pencil-boil",
  "version": "0.9.1",
  "description": "Hand-drawn boil utilities for SVG and Vue 3.",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "proof": "node --import ./proofs/loader.mjs proofs/boil-guard.proof.ts && node --import ./proofs/loader.mjs proofs/frames.proof.ts && node --import ./proofs/loader.mjs proofs/cache.proof.ts && node --import ./proofs/loader.mjs proofs/prebake.proof.ts && node --import ./proofs/loader.mjs proofs/celestial.proof.ts && node --import ./proofs/loader.mjs proofs/raster-serialize.proof.ts && node --import ./proofs/loader.mjs proofs/hold.proof.ts",
    "test": "npm run check && npm run proof",
    "proof:browser": "playwright test"
  },
  "keywords": [
    "svg",
    "animation",
    "hand-drawn",
    "wobble",
    "boil",
    "vue"
  ],
  "author": "mkbabb",
  "license": "Unlicense",
  "engines": {
    "node": ">=24",
    "npm": ">=11"
  },
  "packageManager": "npm@11.12.1",
  "peerDependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "^7.0.2",
    "vue": "^3.5.39"
  }
}
